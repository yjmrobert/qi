# Git-secrets configuration for qi project
# This file contains patterns to exclude legitimate configuration variables
# from being flagged as potential secrets

# Exclude legitimate configuration key patterns
local\s+key=
QI_CONFIG\[.*key.*\]
key="\$
key.*xargs
grep.*key=
Configuration.*key
metadata.*key
case.*key.*in
for.*key.*in
while.*key
IFS.*key
echo.*key=
log.*key=
sed.*key=
if.*\[\[.*-n.*\"\\\$key\"

# Exclude test patterns
test.*key
example.*key

# Exclude documentation examples
docs/
README
\.md:
# Password:
# Username:
your-.*-token
your-username